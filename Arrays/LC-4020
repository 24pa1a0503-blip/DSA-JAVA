class Solution {
    public int elevatorRequests(int n, int[] requests) {
        int s=requests[0];
        for(int i=1;i<requests.length;i++){
            int val=requests[i-1]-requests[i];
            s+=Math.abs(val);
        }
        return s;
    }
}
